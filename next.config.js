/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  i18n: {
    locales: ['es'], // Lista de idiomas soportados (en este caso, solo español)
    defaultLocale: 'es', // Idioma predeterminado
  },
}

module.exports = nextConfig



